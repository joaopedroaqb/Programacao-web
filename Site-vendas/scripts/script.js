// Captura o botão pelo seu ID
const botaoInterativo = document.getElementById('botao-interativo');

// Adiciona um ouvinte de evento de clique no botão
botaoInterativo.addEventListener('click', function() {
  // Exibe um alerta quando o botão é clicado
  alert('Você clicou no botão interativo!');
});
